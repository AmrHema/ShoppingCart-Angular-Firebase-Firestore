export interface userInfo {
    email?: string,
    name?: string,
    isAdmin?: string,
    password?: string,
    address?: string;
    uid?: string,
    displayName?: string,
    photoURL?: string
}